# Genuine secrets only — everything else (city, intervals, models, ports) lives in
# argus_agent/src/constants.py.

FORTYGUARD_API_KEY=your_fortyguard_key_here

# Reasoning (RESPOND stage) — gpt-5-mini
OPENAI_API_KEY=your_openai_key_here

# Fast/cheap chat for sub-agents — Groq, OpenAI-compatible API, free tier available
GROQ_API_KEY=your_groq_key_here

# MongoDB connection string (Atlas or local) — db/collection names are in constants.py
MONGO_URI=mongodb://localhost:27017
